# Airwave — CLAUDE.md

The project guidance for this repository lives in **[AGENTS.md](AGENTS.md)**.

`AGENTS.md` is the tool-neutral convention, so every agent working in this
repo reads the same document. This file exists only so Claude Code picks that
guidance up automatically; it deliberately holds no content of its own.

If you are updating project instructions, edit `AGENTS.md`, not this file.

@AGENTS.md
